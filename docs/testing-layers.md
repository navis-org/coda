# Testing layers

Which test file covers what — the index to consult before writing a new test.

Moved verbatim out of `CLAUDE.md`.


## Testing layers

| File                                     | Covers                                                                                                                           |
| ---------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `core/graph.test.ts`                     | topo sort, cycles (incl. two wires between one pair), serialisation, lenient loading                                             |
| `core/ports.test.ts`                     | variadic ports: expansion and tuple ordering, the three resolution modes, what registration refuses, an arity that shrinks pruning its edges, a file naming a socket past the arity, and auto-wire filling one port per group |
| `core/reference.test.ts`                 | reference edges: the round trip sorting, the identity without its own schema, evaluate not waiting, and that a real cycle and two wires between one pair are unchanged |
| `core/scheduler.test.ts`                 | (also) loops: one pass per element and per group, `First N`, the region waiting for a branch that joins it from outside, a Collect gathering every pass rather than the last, that a settled loop iterates nothing on a second Run, that a failing element does not end the loop, that the auto pass defers a loop whole, and that running one leaves the document byte-identical |
| `nodes/flow/forEach.test.ts`             | the loop node on its own: that `loopPlan` and `evaluate` agree about which element pass *n* means, that a pass is named by its id rather than its cell type, group mode's first-appearance order and its single `(none)` group, that past the end emits nothing rather than the nearest element, and that `registerNode` refuses half a loop |
| `nodes/transform/selectOne.test.ts`      | (also) what a slice re-measures and what it carries: bounds re-measured because they describe the selection, while units, space and the skeleton route describe where the coordinates came from and taking one neuron out cannot change them |
| `nodes/transform/stackNeurons.test.ts`   | (also) the skeleton route kept only where both sides agree — compared by id, since two fetches produce equal objects at best — because two routes in one collection is no route, the rule `MeshDetail` already follows |
| `nodes/flow/collect.test.ts`             | the fold: stacking every pass, a column only some passes carried kept as null, an empty pass folded in without loss, and two passes of different kinds refused by naming the pass |
| `ui/zip.test.ts`                         | the archive read back from its *central directory* rather than its local headers — names, contents, a path, the CRC pinned to the standard check vector, the entry-count refusal, and byte-identical output for a fixed stamp |
| `ui/fileSink.test.ts`                    | which route a loop takes and what a name survives: a folder handle driven against a fake (every writable closed, path segments created, `..` unable to climb out), the dismissed picker answering undefined, and the armed sink claimed exactly once |
| `ui/useForEach.test.ts`                  | a pass's files: named by the element with a zero-padded ordinal, a Download outside the region left alone, `On run` honoured, and a pass with nowhere to write reported rather than thrown |
| `core/scheduler.test.ts`                 | hybrid eval, caching, invalidation, errors, targeted runs, partial results — that `ctx.publish` is visible while a node runs, is replaced by the real result when it settles, and is dropped rather than left standing when it fails — and `ctx.warn`: readable while running, kept with the cached result, deduped, dropped on a failure |
| `nodes/lib/tableOps.test.ts`             | each op, plus schema/value agreement — including the coalesce's blank-is-absent rule, its widening, and backfill-in-place vs append — and the id bridge: a wide id kept exactly, a rounded one skipped, ids ordered by magnitude |
| `nodes/lib/tableOps.test.ts`             | (also) the join's four directions: which right rows count as unmatched, the key column filled from the side the row came from, the widening only where it can bite, and the columns in one order whichever way it runs |
| `nodes/lib/linkageOps.test.ts`           | (also) the mixed-dataset cut: a one-dataset group split where a count cut would keep it, the share threshold, the dataset count that makes an unqualified tree knowable, and leaf-order numbering shared with the other two modes |
| `nodes/lib/edgeComparison.test.ts`       | the L2a algorithm: a real absence told from an unasked question in both directions, the pool read from the mapping rather than the edges, `minWeight` dropping a row and never a value, the widest totals ratio, out and in kept apart, and the derived schema agreeing with its values at every arity |
| `nodes/lib/tableOps.test.ts`             | (also) the relabel: the mapping's value column deciding the dtype, `keep` widening and dropping the unit, a repeated key used once, a null as its own key, a number matching its text, the result name suffixed except where it is the column's own |
| `nodes/lib/tableOps.test.ts`             | (also) the unpivot: row-major order against `pandas.melt`'s, the shared dtype and unit and where they widen, a colliding output name yielding rather than displacing, `Drop empty` keeping a zero, the neurons kind following the id column, and both ceilings met on a shape small enough to build |
| `nodes/lib/tableOps.test.ts`             | (also) the rename: dtype and unit riding along, a swap, both collisions suffixed, the kind promoted only on an applied rename, and `renameMapping`'s resolved-vs-typed branches |
| `nodes/lib/describeOps.test.ts`          | the per-column summary: schema/value agreement, the constant output shape, an empty string counted as missing while `false` is not, the five-number spread against known values, a NaN present but out of the sort, every statistic blank on a text or boolean column, the id column counted and never measured, and the memo's identity |
| `nodes/output/describe.test.ts`          | the Describe node's shape: the tap first and by identity, neurons-ness on the tap and never on the summary, and the summary port typed exactly with nothing wired to it |
| `ui/viewers/describeViewer.test.tsx`     | which port the card draws — the summary rather than the table that passed through, and every other table node still drawing its own value |
| `data/mock/generate.test.ts`             | determinism, internal consistency, source semantics                                                                              |
| `wizard/wizard.test.ts`                  | every combination the Workflow Wizard can build: inference-clean on every dataset, run end to end on the synthetic one, the overview note's markdown parsing, which card each stage hint is docked to, and the caps a generated search carries |
| `data/zoo/zoo.test.ts`                   | the Zoo's two halves and the seam between them: the layout digest (two wires between one pair collapsing to one line), `requires` derived vs declared, a dropped node type as an error against a renamed param as a warning, the reader dropping one bad entry rather than the list, edge indices remapped when a layout node goes, a path that leaves the repository, and the fetch — raw and nothing else, the cache, the stale fallback, and a graph re-fetched on a changed revision rather than on a clock |
| `ui/App.smoke.test.tsx`                  | real app mounted and driven: Run, live filtering, link rejection, undo, per-node run                                             |
| `ui/viewers/viewers.test.tsx`            | chart geometry: bar cap, rounded end, 2px gaps, legend rules                                                                     |
| `ui/tour/tour.test.tsx`                  | (also) the third tour: the graph it builds and both wires coming off Explore's `Selected` (a wrong port id makes `connect` refuse in silence), the cell **order** that is the composition — order is position on a dashboard, so the layout is checkable without layout — the half/full/half spans read through `DEFAULT_ROW_SPAN` since a half-height cell stores its height as absence, and the token warning being raised in `prepare` before the first step touches anything. The interactive-step contract is now `describe.each` over every tour that hands the reader a move |
| `ui/panels/fuzzy.test.ts`                | match/ranking behaviour, including boundary alignment                                                                            |
| `ui/panels/palette.test.tsx`             | command `disabled` flags vs live state, type-filtered node items, Space flow                                                     |
| `ui/notify.test.ts`                      | run notifications: the three runs that say nothing (cancelled, under the floor, nothing executed), the loop passes a node set cannot report, the duration spelled as the status bar spells it, the graph name kept out of the tab title, and the flash restoring the *original* title across a second flash |
| `ui/panels/overlay.test.tsx`             | expand/close paths, rail params, and that restyling does not stale a node                                                        |
| `core/dashboard.test.ts`                 | the dashboard model: one cell per node however many times it is asked (and again on load, where a hand-edited duplicate is two live renderers), the move index counted after the lift, spans clamped and a span of one stored as absence, every mutator unchanged by identity, no trace on a graph nobody has put a node on, and the layout surviving a save and a load while losing the cell for a node the load dropped. Also the view flag: written only when true, unable to be recorded on a graph with no dashboard, surviving every edit that reshapes the layout around it (`setColumns` is the one that could drop it), and a hand-edited truthy value not counting as a decision |
| `ui/dashboard/gridGeometry.test.ts`      | the two sums a gesture needs: the resize's `+ gap` before rounding (without it the grip reads as sticky), the unit measured from a cell that already spans several tracks, a degenerate rect keeping the span rather than collapsing it, the row track height dividing an area so the tracks and their gaps come to exactly it (and the four heights *tiling* it, which a nominal-fraction assertion would not catch), and the drop index's two conversions — after the lift, and which half of the target — plus a drop on the dragged cell resolving to its own place |
| `ui/dashboard/dashboard.test.tsx`        | the grid, mounted: the canvas *gone* rather than covered, the pin dropped on the way in, a cell drawing the node the overlay would and standing down while the overlay owns it, ✕ removing the cell and never the node, the per-cell rail leaving the node unstaled, the add menu offering only unplaced nodes, the drop layer existing only mid-drag, the four row heights snapped to from any span, the app shortcuts reaching the inspector, the assistant and the dashboard *with the canvas unmounted* (the bug `F` reported) while a modified key and a key typed into a field are left alone, and the saved view — opened whichever way the file was saved, recorded whichever order the cells and the mode arrive in, writing nothing on a graph with no dashboard, and costing no undo step |
| `ui/panels/dock.test.tsx`                | the pinned viewer dock: that one node cannot be live in both the dock and the overlay while two *different* ones can, that the card behind it stands down, the three ways a pin is dropped (unpin, node deleted, graph loaded), `P` moving the dock rather than closing it, and the width — clamped at both ends, NaN-proof, published to `.app` as the percentage the grid column reads |
| `ui/params/paramGroups.test.ts`          | tabs/rows reshaping: that the panel shows every param the flat rail did, exactly once; and the card's half — `compare.connectivity`'s tabs growing and shrinking with the arity, the arity control staying in the first one, and the two viewer cards a strip must not reach |
| `ui/nodes/skeletonSource.test.tsx`       | the Skeletons card, mounted: the `Source` dropdown on the card rather than in the inspector, naming the route Automatic will take even on a single-route dataset (an empty control reads as broken and a bare "Automatic" names nothing), and the footer naming the route that answered after a run |
| `ui/nodes/paramTabs.test.tsx`            | the card's tab strip, mounted: that it appears past two tabs and not below, that pressing one swaps the band, that the union over the tabs is the flat band (a lost control looks like a node with fewer settings), and that the selection survives the arity coming down |
| `ui/params/columnField.test.tsx`         | what a column picker says about a port with no schema: the value it will use rather than "(missing)", never disabled, and both true claims kept |
| `ui/export.test.ts`                      | CSV quoting/nulls/chunking, wide-matrix CSV, filenames, standalone SVG                                                           |
| `export/python/export.test.ts`           | both fixtures against their goldens, every wired port declared, every emitting type reached, and a neuPrint cell refusing on a CAVE dataset |
| `export/python/export.test.ts`           | (also) the population: a lone `traced` pushed into `NeuronCriteria`, anything else masked instead (it ANDs and has no null test), Explore always masking since its `All` port *is* the index, the OR parenthesised per disjunct, and an explicit status row winning — the one failure an export can make that nothing else catches: a notebook that runs cleanly and returns a different set of neurons from the canvas |
| `export/r/export.test.ts`                | (also) the same, checked separately because R expresses the narrowing its own two ways — a dplyr `filter()` after `neuprint_search`, and a `WHERE` inside Explore's hand-written Cypher — plus the OR parenthesised per disjunct and the non-empty test spelled out rather than left to `nzchar` |
| `ui/panels/notebookExport.test.tsx`      | (also) the warning on the Save item: one per format, naming the steps, and silence on a graph that exports whole |
| `ui/panels/palette.test.tsx`             | (also) that the export rows' peek starts no walk, and light up once one has run |
| `ui/format.test.ts`                      | that an id column prints verbatim while the count beside it groups, that an aggregate of one is a quantity again, and the length ladder: the rung asked of the rounded figure, any rung read as a source unit, and a sub-nanometre length not rounded away |
| `ui/panels/nodeBrowser.test.tsx`         | rows/thumbnails/signatures, chip-search exclusivity, entry points                                                                |
| `ui/panels/zoo.test.tsx`                 | the Zoo browser: what a row says about a token, chip/search exclusivity, the replace-the-canvas question and that a failed fetch does not ask it, a drifted graph opening with a hole and a notice, all three offline states, and that a deposited README does not render an image |
| `help/help.test.ts`                      | the help documents against the registry: every file names a real node, every figure builds with no problems, every cross-reference points at a documented node, every image exists — plus the figure grammar, the layout, and that the extended markdown kinds stay off without the flag |
| `ui/help/helpOverlay.test.tsx`           | the `?`: present for a documented node and absent otherwise, a figure drawn from the registry rather than from the document's text, the focus ring, a cross-reference followed in place and come back from, and a node with no document saying so |
| `ui/encoding.test.ts`                    | palette rules: the cycle past the end of a palette against `foldByRank`'s Other, area scaling, null handling, the `hash` mode's departures, and the five palettes' sizes, distinctness and rotated order. Also `resolveShape`: frequency ranking, the fold onto a dash that reuses no mark, `Other` as the folded key, overrides and the guard on one that names no drawable shape, and that `SHAPE_OPTIONS` offers exactly `ALL_SHAPES` |
| `data/geometryCache.test.ts`             | the session geometry cache: asks for exactly the missing ids, never calls the fetcher with nothing, hands back the same array rather than a copy, keeps two levels of detail apart, LRU by bytes, the age a hit reports, and streaming — publishes in the caller's order, rate-limits, waits for `readyBefore` (and publishes anyway when it rejects), says nothing on a full hit, and caches each body as it lands |
| `ui/segmentColor.test.ts`                | neuroglancer's segment hash, pinned to specific ids: the Murmur round, uint64 halves, the seed, the 18-digit id that a float64 would round into its neighbour |
| `ui/viewers/networkLayout.test.ts`       | topology reading, layering (incl. cycles), every layout, and the prefuse branch: components get disjoint boxes at **forty** triangles (fewer passes with the feature disabled), and `componentLabels` agrees with `networkOps.connectedComponents` |
| `ui/viewers/prefuseForce.test.ts`        | prefuse's simulation: the Barnes-Hut walk reproduces the exact pairwise sum at theta 0 and degrades smoothly as it opens, coincident points do not hang, determinism, nothing goes NaN |
| `ui/viewers/componentPack.test.ts`       | shelf packing: boxes never overlap (with and without a gap), the field contains them, roughly square, order-independent, zero-sized boxes |
| `ui/viewers/networkRebuild.test.tsx`     | that restyling never re-runs the layout — the camera-reset regression, counted                                                   |
| `ui/viewers/layoutMemo.test.ts`          | that a settled layout returns only while it still describes the graph                                                            |
| `ui/viewers/markGeometry.test.ts`        | that the generated GLSL and the polygons describe the same marks — sampled over a grid — that the triangle points up in both (the y flip), that no mark escapes the quad the shader rasterises, and that the shader's numbers come from the constants |
| `ui/viewers/networkDraw.test.ts`         | reciprocal curvature, arrow geometry, the exported SVG — including that a circle keeps its own primitive and every other mark goes through the scatter's `markPath`, so the two exports cannot disagree |
| `ui/viewers/networkStyle.test.ts`        | focus/ego sets, dimming (hue kept, theme-flipped), and what a tooltip decides to say                                             |
| `ui/viewers/networkDrag.test.ts`         | which nodes a grab picks up (selection, or just the one), the delta that keeps the grab offset, the click/drag slop              |
| `ui/viewers/networkSelect.test.ts`       | the context menu's four scopes, the anchors a right-click acts on, direction ignored on an undirected network, and the answer in node order|
| `ui/viewers/networkContextMenu.test.tsx` | which rows the menu offers: the directed pair only on a directed network, the whole-graph verbs only off a mark, Clear dead with nothing selected|
| `ui/viewers/networkColor.test.ts`        | the two network-only colour modes: one colour per component and never two, the largest component in the leading slot, a link taking its endpoint's exact colour, overrides included|
| `nodes/query/roiMeshes.test.ts`          | the region-mesh node: omitted-versus-empty `rois` at the source seam, the picker's options coming from the dataset, the unknown-region and size refusals |
| `nodes/output/viewer3d.test.ts`          | the 3D node's shape: the deliberately empty card against everything still reaching the inspector, the tab-per-socket arrangement, the hash colour default, the per-socket switches, opacity as a facet of the colour row |
| `ui/viewers/cameraMemo.test.ts`          | the camera that outlives its component: per viewer, absent until framed, forgotten on reset, LRU by last write |
| `ui/viewers/sceneMemo.test.ts`           | the neuroglancer state that outlives its iframe: per viewer, gated on base and identity, forgotten on reload, LRU by last write |
| `ui/viewers/liveRenderers.test.tsx`      | one live renderer per node: the inspector standing a WebGL viewer down, a card dropping its preview while the overlay owns it, and the streaming precondition — a 3D scene drawing from its input port before its own node has run, without drawing an empty box for a graph that never ran |
| `ui/viewers/viewer3dScene.test.ts`       | the 3D viewer's whole testable half: segment building, the colour and dimming buffers, the one-pass key→ids index, hit→neuron, the opacity/`depthWrite` pair, framing in the recentred space, the pinned background, the legend's four gestures (visibility, key→ids, select-whole-key, hide/solo), and both caption notes — the mesh level of detail and the skeleton route, each absent rather than guessed where the value names none |
| `ui/viewers/networkViewer.test.tsx`      | the caption: counts, the label-thinning admission, size refusal                                                                  |
| `data/neuprint/neuprint.test.ts`         | Cypher building/escaping, response decoding, both halves of the `bodyId`→`neuronId` seam, schema discovery, mesh-source resolution, nm conversion |
| `data/neuprint/neuprint.test.ts`         | (also) the two skeleton routes: the *volume* resolved where the meshes live in a sibling (the case that would conclude a dataset publishes none), the peek starting both reads and answering `undefined`, asking once however many times inference runs, and the SWC leading the list once the bucket answers |
| `data/neuprint/live.test.ts`             | which datasets actually publish a skeleton layer — five of twelve, listed — and the claim the dropdown makes, checked: the same neuron down both routes at the same node count and the same nanometre bounds, radii on the SWC and none on the published copy, and the published route refused rather than answered with the SWC on a dataset that has none. Skipped without `NEUPRINT_TOKEN` |
| `data/precomputed/precomputed.test.ts`   | shard lookup, multi-LOD manifest, Draco decode, legacy fragments, CORS fallback, and the two-phase progress split every mesh source reports through |
| `data/precomputed/datasource.test.ts`    | a precomputed URL as a datasource: what an `info` is classified as, a volume followed to its mesh directory — **including one that declares no `@type`**, which is the shape every pre-convention flat segmentation takes and was being read as a legacy mesh directory at the bucket root — a named mesh directory with no `info` of its own opened as legacy while a 503 on the same URL is still a failure, flat meshes told from multi-resolution ones, each `info` read exactly once so the first Run costs no requests, an unsharded directory not advertised as geometry the Run would refuse, the sidecar read into named columns and dropped when its values do not line up with the ids, the region list filling only once asked, a failure remembered so a `cheap` node cannot re-ask per peek, retry re-reading only a failure, the optimistic capability before the probe lands and the honest one after, and both refusals naming a way in |
| `data/precomputed/skeletons.test.ts`     | the skeleton format and the three things its bytes do not say: an edge list read as a rooted tree in visit order, a cycle broken, one root per component, a `radius` found behind another attribute and declined in a width it has no scale for, the full affine applied, and cable length not measuring the gap between components |
| `data/precomputed/live.test.ts`          | the same against the real buckets, skipped without `PRECOMPUTED_LIVE` — male-CNS followed to `multi-res-meshes` rather than the single-resolution directory its viewer state advertises, hemibrain's segment-property sidecar found, one real body decoded in nanometres and keyed by text, male-CNS skeletons read as trees with the same body's mesh and skeleton boxes overlapping (the check that catches a factor-of-eight in either reader), hemibrain's unsharded region shells fetched by name, its 22,706-neuron index listed and queried, and a segmentation that publishes no meshes reported as one |
| `data/neuroglancer/sourceUrl.test.ts`    | source specs: the three spellings of one address collapsing onto one canonical string (the registry key), the location keeping its own scheme, `middleauth+` stripped, and `precomputedToHttp` staying narrower than the parser on both axes |
| `nodes/transform/updateRootIds.test.ts`  | the repair: only stale rows looked up, supervoxels sent as raw uint64, a current row left alone even with a warm cache, a row with no supervoxel untouched, the id column's declared dtype kept rather than row zero's, both pickers resolving before any schema has arrived — and a Dataset from another backend refused on the card, naming the backend rather than the colon it used to fail on |
| `data/cave/oauth.test.ts`                | signing in, minus the window: the login prefix read off `auth_info` rather than assumed (`/sticky_auth`, not the `/auth` every document names), a relative `login_url` refused rather than resolved against Coda's own origin, a deployment publishing no login service told apart from one a browser could not read at all, an abort staying an abort, the terms-of-service arm's bare `"success"` not read as a token, and an identity lookup that degrades to no name rather than failing a sign-in that worked |
| `data/cave/cave.test.ts`                 | (also) the sign-in label: an account name stored beside a token that was signed in for, and **cleared** when one is pasted, since a pasted token wearing the last account's address is the one thing on screen saying which of two Google identities is in use |
| `data/cave/rootIds.test.ts`              | the drift check: ids sent as unquoted integers, the frozen instant read as UTC rather than local, asked once per chain and re-asked when the wiring changes, a late lander not overwriting a newer answer, only the unseen ones asked, and nothing at all without a chunkedgraph |
| `data/catmaid/catmaid.test.ts`           | CATMAID against recorded bodies: the confidence-bucket sum, parents rebuilt as indices, the indexed list encoding, an anonymous POST refused a direct route, a status filter ignored and a region filter refused, and a thumbnail answered with the *skeleton* — through the same decoder, so a tile cannot draw a different tree than the Skeletons node does |
| `data/catmaid/publicTokens.test.ts`      | Virtual Fly Brain's published anonymous tokens: the snapshot covers every instance including the default server's *other* hostname, no token reaches a host nobody published, a configured token wins over the published one, the manifest refresh is cache-bypassing and once-per-session and leaves the snapshot alone when unreadable, a rotated token falls back to anonymous instead of failing harder than no token at all, and the refresh fires from a request that uses a token rather than from the app entry |
| `data/catmaid/live.test.ts`              | the same source against the real VFB FAFB instance, skipped without `CATMAID_LIVE` — a skeleton proved to be one tree in nanometres, its synapses inside its own bounds, the neuropil volumes parsed, and a real 16,840-node thumbnail skeleton at the size that costs (940 kB for skeleton 16, 4.2 MB for 2333007) |
| `data/cave/cave.test.ts`                 | CAVE against recorded bodies: a wide root id kept exactly, the string-aware scan, the annotation pivot, an anchored pattern, a size filter refused rather than emptying the result, and every refusal |
| `examples/starters.test.ts`              | (also) the BANC starter: it builds with no issues at all, its reference edge leaves `topoSort` acyclic, the CAVE table's pivot params, and the generic half asserted against `genericStarter`'s own edges so the composition cannot drift |
| `data/cave/cave.test.ts`                 | (also) the dataset card: the four spec bindings appended to the publisher's blurb and marked as Coda's, an unbound role spelled out rather than left off, and `species` read off the spec so a mouse is not a fly |
| `data/cave/cave.test.ts`                 | (also) truncation: what `refuseIfCapped` does with a count and without one — including the case that broke, a *complete* answer larger than `CAVE_MAX_ROWS` — and that the count carries the read's filter and nothing else. The stub answers `count=true` by counting its own rows fixture, so the two halves cannot drift into agreement |
| `data/annotations/annotations.test.ts`   | (also) reference tables: the join endpoint rather than the single-table one, `select_column_map` rather than `select_columns`, the count going to the base table, a wide read naming its own columns, and a table with no `reference_table` left exactly where it was. Its BANC stub is `installBancFetch` and deliberately not `src/test/caveStubs.ts` — everything here turns on the request *body*, where that one answers by URL |
| `data/cave/cave.test.ts`                 | (also) the skeleton service: the route list in preference order as each peek lands (offered even with an empty cache, because whether it can answer is a question about *these* root ids), `exists` asked before anything is fetched, the service taken only when it covers every neuron, the fallback to the chunk graph when it holds nothing, a pinned route refused rather than substituted, and `middleauth+` read as an instruction rather than an address |
| `data/cave/cave.test.ts`                 | (also) the two morphology routes: the flat pyramid preferred over graphene where a materialization has one and the meshing API never asked, published skeletons taken over the chunk graph, `capabilitiesFor` flipping to `skeletons: true` once the bucket answers on a datastack with **no** L2 cache, the refusal naming both routes when neither exists, a thumbnail drawn from the pyramid and declined for graphene, and a recently edited fragment read from `unsharded_mesh_dir` while a sharded one stays at the mesh root |
| `data/cave/cave.test.ts`                 | (also) discovery: the tables listing on a **v2** path and the views on a v3 one, no views request when the toggle is off, one fetch however many peeks, the two row counts kept apart, a view described from the listing with neither a metadata nor a count request issued, a declined count leaving a row off rather than failing the node, the facts peek gated on the listing so a half-typed name requests nothing, and one sampled row read into a column listing |
| `nodes/lib/datasetFamilies.test.ts`      | (also) that every CAVE family names a datastack spec and every spec a family — the join key nothing else checks; and that the two CATMAID families, which are both project `1`, name two registered sources |
| `nodes/dataset/caveTables.test.ts`       | the two CAVE discovery nodes: a schema that does not move when Include views is toggled, tables sorted before views, the wire winning over the field, a wide id reaching the column listing as exact text and a null-sampled column admitting no dtype, the warning before a view is sampled and the sample happening anyway — and both halves of "which datastack" refused on the card, plus an unknown table staying silent until the listing has landed |
| `ui/nodes/caveTableInfoBody.test.tsx`    | the CAVE table info card: both row counts drawn with the label saying which is which, a view drawn from the listing with no counts at all, the permissions held back until the overlay, and the five absences said apart — two fetches means two different stalls |
| `data/cave/live.test.ts`                 | (also) where a deployment signs in: `auth_info` answering unauthenticated, naming a login service on its own origin at middle_auth's endpoint, and Google behind it. The prefix is deliberately not asserted — reading it is the point — and this is the only thing that would notice `login_url` moving |
| `data/cave/live.test.ts`                 | the same source against the real services, skipped without `CAVE_TOKEN` — the only thing that notices an endpoint shape changing, the mesh and synapse clouds proved to share one nanometre frame, Aedes' edge list built by counting with nothing configured, and a loadable scene assembled for all three datastacks |
| `data/cave/live.test.ts`                 | (also) BANC, on a **second deployment**, which is what makes the row cap visibly per-server: one table holding more rows than `CAVE_MAX_ROWS` and returning all of them, a reference table's root id arriving bare from the join, the base count matching the joined rows, and the 500 that a single-table read still answers |
| `data/cave/live.test.ts`                 | (also) the flat segmentation beside a materialization: `gs://flywire_v141_m783` resolving to a **volume** rather than a legacy mesh directory despite declaring no `@type`, 630 publishing meshes and no skeletons where 783 publishes both, a version nobody flattened having no entry, one neuron's coarsest level fetched inside `THUMBNAIL_MAX_BYTES`, and its published skeleton read as a rooted tree. The mesh and the skeleton bounding boxes are asserted against the same nanometre range — the pair is what would catch a missing conversion on either, since each is internally consistent alone |
| `data/cave/live.test.ts`                 | (also) the skeleton service against two datastacks, which is the only way to see the gap between declaring one and having one: FlyWire's answering `exists: false` for every id and being remembered as empty for the session, and minnie65's answering real reconstructions — thousands of nodes with radii — where the same datastack's level-2 route gives a few hundred. The route list is asserted *after* awaiting the L2 peek, because the fetch that took the service never asked it |
| `data/cave/live.test.ts`                 | (also) graphene, now exercised through **BANC** rather than FlyWire, because FlyWire takes the flat route: a real manifest decimated to the triangle budget, which is the only route where `Detail` is a continuous knob rather than a choice of published level |
| `data/cave/live.test.ts`                 | (also) a thumbnail off BANC's real level-2 chunk graph, which is the only cheap geometry a `graphene://`-only datastack has. Asserted in the data layer's own terms — edges present, extent non-zero — rather than by rasterising: `src/data` does not import from `src/ui`, and the test-file exemption in the lint config is for helpers rather than a way around invariant 1 |
| `data/annotations/annotations.test.ts`   | annotation sources: the `Token` scheme, the pseudo-workspaces dropped, a wide id kept as text, the outer join and the later source winning, a rename collision suffixed in all three shapers, the route fallback's three rules — and the Sheet link grammar, its cache keyed on the tab rather than the ref, and which status blames what |
| `data/annotations/live.test.ts`          | the same against real FlyTable, skipped without `SEATABLE_TOKEN` — including the ids proved to be beyond double precision — and Google Sheets behind `GOOGLE_SHEET_LIVE`, where what is asserted is the CORS header nothing else can see |
| `nodes/annotation/annotations.test.ts`   | the four source nodes: the two halves of one join asserted against each other, half a chain published as nothing, a Select in the chain (the case that decides the socket type), a table with no `neuronId` refused twice — and the Sheet link's tab reaching the ref, a refusal worded once for the badge and the run, a column named before the tab has been read, and both halves of `datastack:materialization` refused on the card: a Dataset from another backend, and a typed datastack with no version |
| `nodes/query/morphology.test.ts`         | the shared `Warn above` threshold, that a large set warns and still fetches, and what the message blames                          |
| `nodes/query/morphology.test.ts`         | (also) the shared route refusal, driven through a source that only ever has one route — the gap where three of the five sources read no route at all and silently answered with their own |
| `nodes/query/morphology.test.ts`         | (also) the Skeletons node's `Source`: the route named on Automatic even where there is only one, each route offered once there is a choice, a stored choice kept rather than called broken while the peeks are in flight, a pinned route the dataset lacks reported instead of substituted, the empty default reaching the request as `undefined` (which is what lets a source fall back), and the param staying out of `presentational` so the route reaches the provenance key |
| `ui/nodes/nodeRunRing.test.tsx`          | run-indicator arithmetic: dash fractions, the zero floor, indeterminate mode                                                     |
| `ui/nodes/runRing.placement.test.tsx`    | that the outline renders outside the clipped card (slow mock, so 'running' is observable)                                        |
| `nodes/analysis/network.test.ts`         | BuildNetwork semantics + the selection round-trip                                                                                |
| `nodes/analysis/nblast.test.ts`          | the units conversion above all, plus the flattening, the labels, the pair-count warning and the one allocation floor still refused, the voxel refusal, and every control reaching the request |
| `nodes/analysis/nblastKnn.test.ts`       | the k-NN shape: the rectangle laid out long, the padding dropped and counted, and the id columns named so a neuron id is not a quantity |
| `nodes/analysis/synblast.test.ts`        | the grouping above all — a point cloud is one row per *synapse*, so first-appearance order, orphan connectors as their own group, and ids keyed as text — plus the micrometre conversion, the polarity mapping, and `by_type` going off when the picker is cleared |
| `nodes/analysis/nblastMatches.test.ts`   | invariant 3 for the three modes, the `auto` direction resolved off `measure` with an unstated one meaning higher-is-better, `n` clamped rather than raised on, the diagonal refusal on a rectangle, and fastcore's padding never reaching the table |
| `nodes/transform/cleanSkeletons.test.ts` | the item count above all — an emptied neuron stays in the collection — plus parents kept neuron-local both ways, the micrometre controls converted where the coordinates are not, the voxel refusal *only* where a distance is in play, and the resample allocation floor |
| `nodes/transform/cleanMeshes.test.ts`    | face indices kept mesh-local both ways, the item count, `detail` dropped wherever the face count could have moved, and the ray-cast warning that never refuses |
| `core/values.test.ts`                    | what a node footer says about a geometry value: its units printed always, voxels-vs-unknown told apart, and the skeleton route named ahead of the space and the units — the one thing about a skeleton set a count cannot imply |
| `nodes/lib/linkageOps.test.ts`           | the tree ops: the square-but-not-one-population refusal, exactly-k against a tie, clusters numbered in leaf order, the copied buffer |
| `nodes/lib/similarityOps.test.ts`        | the five metrics against **scipy's own** `pdist` on the dense form of the same vectors — the one comparison that can catch a shared mistake — plus presence applied after the merge rather than by ones, the repeat coalescing, both layouts agreeing, the exact pair-work count, and the crash-floor refusal |
| `nodes/analysis/matchTypes.test.ts`      | the node around the mapper: both port sets growing together with the count, every port typed before a run, the report's schema staying constant while the arity does not, the refusal naming a source with no neuron index — made *before* anything is fetched — every numbered input being a Dataset, the unmatched-fraction attribution, and one mock dataset through both sockets end to end — including the Pass Through port, exercised on the one configuration where it shows (`type` against `instance` corresponds nothing, so the whole report is pass-through) |
| `nodes/lib/typeMapping.test.ts`          | the cross-dataset cell-type mapper against cocoa's own worked examples: both AOTU008 figures — the subtypes surviving two hemispheres and collapsing the moment a third dataset knows only the coarse label — the four rows of comparative.md's table, the compound guards that must *not* split, `allowIndirect` on and off, all three label modes including the id that two datasets share, the `suspicious` flag, and the oversized component that warns instead of stalling |
| `nodes/lib/partnerVectors.test.ts`       | the reshape: both directions kept apart by the prefix, an edge inside the query set counting for both ends, the wired-Neurons and derived-from-`direction` routes agreeing on hop 1 and only one reaching past it, the untyped fallback and drop, per-direction fractions, and the id column's dtype carried through |
| `nodes/analysis/similarity.test.ts`      | the pair through a real Scheduler on the mock connectome: nothing leaking in from the partner side of a `both` traversal, `measure` reaching Linkage, the long schema published before any run, and a hidden param costing no re-run |
| `nodes/analysis/linkage.test.ts`         | the three nodes: every control reaching the request, the cut carried on the pass-through, and a restyle costing no run             |
| `ui/viewers/dendrogramLayout.test.ts`    | the brackets: slot centres, a subtree as a contiguous run, a branch coloured only where its leaves agree, and both orientations     |
| `ui/viewers/tooltipPoint.test.ts`        | a tooltip's coordinates: relative to its containing block and divided by the zoom, plus that the stylesheet still says `absolute`   |
| `nodes/lib/labelsToNeurons.test.ts`      | labels to neurons: a type expanding to every neuron carrying it, matched as text, the first row for a repeat, and a collision suffixed |
| `nodes/transform/labelsToNeurons.test.ts`| the two registrations: identical params, one warning that differs, and a neurons type published for a 3D socket                     |
| `ui/nodes/labelsToNeuronsBody.test.tsx`  | the readout: what a wrong `Match on` says, not-wired against not-run, and every non-advanced param drawn                            |
| `ui/viewers/dendrogram.test.tsx`         | the card through `ValuePreview`: a click handing back exactly the leaves under it, and every admission the caption makes           |
| `data/neuroglancer/scene.test.ts`        | scene editing against the real published shapes, the URL round-trip, and `middleauth+` following the viewer rather than the backend |
| `nodes/output/neuroglancer.test.ts`      | what lands in the link: segments, colour agreement with the 3D view, the limit, and which viewer the segmentation is authenticated for |
| `ui/viewers/neuroglancerViewer.test.tsx` | that a restyle navigates the frame rather than remounting it, and that Reload does the opposite — a fresh document, and no merge into one still booting. Also the card-to-overlay hand-off: the state is read while the frame is still in the document, and the next mount resumes it rather than the published scene |
| `ui/nodes/nodeResize.test.tsx`           | handles outside the clipping card, resize not invalidating a result, gesture undo                                                |
| `ui/nodes/paramFold.test.tsx`            | folding a card's param rows: the header button surviving the band, notes excepted, one undo step, and that it costs no run       |
| `ui/nodes/collapsedPorts.test.tsx`       | collapse to a header: handles moved not removed, each still addressable, and the wrapper's width kept but not its height         |
| `ui/nodes/hiddenParams.test.tsx`         | the `… N more` hint: both counts, the card with no band at all, what is not a change, and that it never closes an open inspector |
| `nodes/lib/neuronSearch.test.ts`         | the Explore Dataset query language: parsing, matching, null rules, the fuzzy fallback, ranking, completion                               |
| `data/cache.test.ts`                     | IndexedDB-less degradation, fingerprint/expiry invalidation, index dedupe                                                        |
| `ui/explore/thumbnail.test.ts`           | silhouette projection/shading, and the data-driven row spec. Also the skeleton rasteriser: every branch of a tree stroked rather than a chain through it, a diagonal drawn *continuously* (the failure a quad-and-fill has), several components not joined through a fabricated edge, an out-of-range parent skipped rather than projecting `NaN`, and its tile bounds agreeing with a mesh's to within a pixel — which is what a shared `fitToTile` buys and two copies would drift on |
| `ui/explore/chips.test.ts`               | that the chip hues in `theme.css` still match `colors.ts`, that a slot follows the field, which chips each backend's vocabulary yields, and a tags column held out of all three lists |
| `ui/explore/neuronRow.test.tsx`          | what a row draws: community tags apart from the chips and unlike them, capped with the rest in the counter's title, an absence drawing no row — and a stat scaled into its own unit with the stored figure kept verbatim on hover |
| `ui/explore/explore.test.tsx`            | the widget: live filtering vs debounced commit, paging staleness, selection, completion, and it mounted in the real editor       |
| `nodes/query/explore.test.ts`            | the node's ports: that `All` ignores both the search and `Max hits`, and infers what it evaluates                                |
| `ui/explore/explore.test.tsx`            | (also) the population read off the dataset **type**, not the value — the card loads before any Run — narrowing the shared index rather than the fetch, and two boxes listing *more* neurons than one |
| `nodes/lib/labelLookup.test.ts`          | label parsing, the typed/wired union, and what the unmatched report refuses to claim                                             |
| `nodes/query/idsFromLabel.test.ts`       | exact vs regex, the anchoring, the union reaching one query, and empty meaning empty                                             |
| `data/filterRows.test.ts`                | what a Find Neurons row means: every operator's lowering (anchoring, escaping, the empty pair), the null rule surviving it, the storage round trip dropping a row still being filled in, and each refusal `resolveRows` reports rather than dropping |
| `nodes/query/findNeurons.test.ts`        | the node through the scheduler: rows ANDing, a set in one row, no rows meaning everything, the legacy params folding into the rows they used to compile to (and contributing nothing at their defaults), and `validate` naming a field this dataset lacks before anything runs |
| `ui/nodes/findNeuronsBody.test.tsx`      | the card: Add drawing a row without writing one, the field list being the dataset's, an operator that cannot survive a change of field, and a legacy node showing its old params as rows and converting them in one edit |
| `layout/placeGuards.test.ts`             | that no bundled graph draws one card on top of another — the one place that can see both `COL_WIDTH` and `NODE_BODIES[type].width`, which are declared either side of the headless boundary and kept in step by hand |
| `ui/nodes/idsFromLabelBody.test.tsx`     | the card: every non-advanced param rendered, and the unmatched line naming the labels                                            |
| `nodes/lib/datasetFamilies.test.ts`      | version ordering, latest-vs-pinned resolution, and the deployment/base-URL mapping                                               |
| `nodes/dataset/dataset.test.ts`          | per-dataset nodes infer what they evaluate, the custom node's lazy source, that the superseded node still runs, the drift advisory following the annotation chain in both directions, and CATMAID's project dropdown: not-listed-yet apart from lists-none, a stored value kept, and a source per instance |
| `nodes/dataset/dataset.test.ts`          | (also) the population checkboxes: offered by the neuPrint families and by no other backend, on the card rather than behind Advanced, the per-**family** defaults (hemibrain typed, male-CNS superclass, everyone else none), reaching the inferred type, an absent param read as off so a saved graph keeps its neuron set, and the warning that stays quiet until discovery lands |
| `ui/nodes/datasetBody.test.tsx`          | (also) the population *summary*: no checkbox on the card at all (they are inspector-only), the applied filters named in one line, several joined, nothing drawn when nothing narrows, a ticked-but-undroppable filter omitted once discovery lands, and nothing reported for a graph saved before the params existed |
| `core/graph.test.ts`                     | (also) `absentMeans`: a stored document with no key for a later-added param recording what it meant, a stored value of either kind left alone, and nothing else filled in |
| `nodes/lib/populationParams.test.ts`     | the middle of the population filters: which columns answer `typed` (a suffix, so `celltypePredictedNt` cannot join the OR and pass every row), absent-vs-false-vs-true, declaration order in the key, a filter dropped where the dataset publishes no column, both warnings, and the local filter — the **union** rather than the intersection, `typed` spanning several columns, an empty string counted as absent, nulls failing every test, the input returned by identity when nothing is asked, **one object per (index, population)** so `searchIndexFor`'s haystack cache holds, and a real index narrowed to a strict non-empty subset |
| `ui/nodes/datasetBody.test.tsx`          | preview above fields, the version dropdown, the resolved id, and no expand button                                                |
| `nodes/dataset/ngsource.test.ts`         | the Neuroglancer Source node at the seam: pasting a URL registering a source the graph can resolve, a Meshes node wired to it advertising `str` ids and refusing an image stack, `validate` staying silent while the probe is in flight, and an explicit Run retrying a remembered failure |
| `data/mock/morphology.test.ts`           | tree validity, determinism, tube meshes, synapse placement                                                                       |
| `store/inference.test.ts`                | inference against a source that has not learned its listing yet, and the re-infer signal                                         |
| `store/groups.test.ts`                   | group frames at the store and in the file: the exclusivity move (and the frame it empties), a member deleted by any route, a card claimed by two frames in a loaded file, a colour that is not one of ours, half a frame declined by a duplicate, and a drag as one undo step |
| `layout/align.test.ts`                   | align and distribute: a size actually used (four of the six edges degrade to a left-align without one), the selection's box as the anchor, distribute evening the **gaps** and ordering by centre, idempotence, an overlap spaced evenly rather than refused, and no move returned for a card already in place |
| `ui/panels/alignTools.test.tsx`          | the grid where somebody reaches it: acting on the selection like Mute, one undo step, no undo step for a press that changes nothing, auto-layout ended, the menu staying open for a second press, the 2-and-3 card floors said in the tooltip, the lock, and the same grid on a frame acting on its members |
| `layout/groupBounds.test.ts`             | the box a frame draws: the padding on four sides, the measurement preferred over the declared size, a zero measurement ignored as the layout ignores it, and a frame whose members are all gone drawing nothing |
| `ui/panels/groupMenu.test.tsx`           | groups on screen: the default drawn as an absence (grey, unfilled), the title appearing only once there is one, the swatches and both toggles, Rename reaching into the viewport-portal layer, Escape reverting it, and all four ways in — node menu on a selection and on an unselected card, ⌘G/⇧⌘G, and the two palette rows |
| `ui/panels/startPage.test.tsx`           | start page: both rails, a tile per card, the replace-confirm, close-vs-dismiss, and both ways back                               |
| `ui/panels/shortcuts.test.tsx`           | the shortcuts card and the table under it: every id its four consumer surfaces ask for, `formatChord` on both platforms, the dialog's rows against the table, and the keys it advertises dispatched at the real window listener |
| `ui/panels/panels.test.tsx`              | (also) the New menu: one submenu per backend rather than per source (FAFB and L1 are two CATMAID servers), an escape hatch inside each, the three volumes held back without being unregistered, no synthetic dataset and no empty group where it was, the row's blurb naming what is in its flyout, and the order under Empty — the wizard above the rule, the Zoo under it |
| `nodes/lib/profileStats.test.ts`         | the profile roll-ups: distinct partners, nested-ROI filtering, the last-parenthesis side rule, NT column matching                |
| `nodes/lib/networkOps.test.ts`           | filter order, ranking after the weight cut, and the recomputed degree roll-ups                                                   |
| `nodes/query/adjacency.test.ts`          | the two outputs describing one fetch: Matrix first, the Links table typed exactly with nothing wired, one row per non-zero cell against the matrix's own count, the chain reaching Build Network — and the picker reality that makes it two dropdowns rather than none, since `net.build`'s `default: ''` means "first compatible column" and lands Source and Target both on `source` |
| `nodes/lib/networkMetrics.test.ts`       | the graph statistics in two layers: hand-worked cases for the decisions (a self-loop in degree and nothing else, clustering null rather than 0 on a leaf, metric columns written over the ones a network carried, density that cannot exceed 1, and the cost and drop count surviving a memo hit so `evaluate` can always warn), and agreement with **networkx** on the definitions — density, reciprocity, per-node clustering, transitivity, degree assortativity, k-core and the component count — read from the checked-in `__fixtures__/networkx.json` so no Python is needed to run it |
| `nodes/lib/networkCentrality.test.ts`    | Brandes on hand-worked paths, the weighted branch making a heavy link a short one, parallel links merged before `sigma` is accumulated, a sampled sweep refusing to report a diameter, PageRank summing to one with a sink's mass redistributed, Louvain numbering largest-first and being reproducible from the seed; then networkx agreement on normalised betweenness, harmonic centrality, weighted PageRank and eigenvector, and the path statistics over reachable pairs |
| `nodes/output/networkMetrics.test.ts`    | the Metrics node's shape: Network first, all three ports typed before a run, the metric columns folded into the pass-through without duplicating `degreeIn`, every setting presentational, and — the one nothing else would catch — the scatter's pickers offered the schema the node is about to *produce* rather than the one arriving |
| `nodes/analysis/networkCentrality.test.ts` | the Centrality node's shape: `expensive`, node columns following the switches while the summary stays constant-width, `centralityOptions` as the single reader that keeps the declared schema and the emitted columns from disagreeing, the refusal when every measure is off, and determinism across two runs |
| `ui/viewers/networkMetrics.test.tsx`     | which value reaches the metrics card — it draws from the node's *input*, so its branch has to sit above `ValuePreview`'s `!value` guard — plus which of the four plots decided it had something to draw, and the tiles that drop rather than print an em-dash on an undirected graph |
| `nodes/analysis/filterNetwork.test.ts`   | the other half of `networkOps`: hop counts and walk direction, a component ignoring direction and stopping at the component, the both-ends rule for an induced link, roll-ups recomputed against the survivors — and around it the node's seed union, filter row ∪ wired table with neither overriding |
| `nodes/output/profile.test.ts`           | that Neuron Profile is a tap, and that paging is free while pinning is not                                                              |
| `ui/viewers/profileViewer.test.tsx`      | the widget: pager clamping, absent tiles vs dashes, the threshold reaching a heading, card-vs-overlay, and the annotation chain reaching both fetches and the cache key |
| `ui/markdown.test.ts`                    | the markdown subset against the real published blurbs, plus what a hostile one cannot do                                         |
| `store/companion.test.ts`                | that a dataset node brings its Description card, as one undo step, and which families opt out                                    |
| `store/autowire.test.ts`                 | that a new node's Dataset socket arrives fed, and every case where it must not — ambiguity, load, an existing wire               |
| `store/fitOnLoad.test.ts`                | that opening a graph asks for a fit, and that an empty one does not leave the request pending                                    |
| `ui/fitOnLoad.test.tsx`                  | that the request is spent: `fitView` called on every open with `nodesInitialized` reading the `false` it really returns, and not called on an ordinary edit |
| `ui/nodes/descriptionBody.test.tsx`      | the card in the real editor: links, nesting, the overlay, and that raw HTML never mounts                                         |
| `store/library.test.ts`                  | the browser shelf against real IndexedDB: name-as-identity, and that a save with nowhere to go _rejects_                         |
| `store/session.test.ts`                  | the open workflows in IndexedDB: order taken from the meta row, a document the order forgot, one tab's session not swept up by another whose id prefixes it, what bounds the stored sessions, and a missing IndexedDB reporting an empty session rather than a failure |
| `store/documents.test.ts`                | more than one workflow open in one tab: which opens reuse a blank canvas and which mint a document, what a switch puts back (graph, selection, undo stack, viewport), that two copies of the *same file* do not share results, and where closing lands — plus the reload seam: the set coming back in order and on the document that was on screen, the active one not restored twice, a switch writing the outgoing document, a close forgetting it, and both directions of the slot-versus-session precedence when a tab has lost its slot |
| `store/autosave.test.ts`                 | the crash net across tabs: a reload landing on its own graph rather than the last written, a fresh tab adopting the most recent, what bounds the slots, and a duplicated tab taking a new identity |
| `ui/panels/library.test.tsx`             | the menus and the start-page rail: the replace prompt, the delete prompt, an opened entry landing on the canvas                  |
| `ui/panels/caveSignIn.test.ts`           | the login window's four endings, through the `openWindow` seam: opened blank *before* the lookup that points it, a message taken only from that window **and** that origin, a closed window settling the promise instead of hanging it, a blocked pop-up asking for nothing, and a deployment that will not say where it logs in leaving no window behind |
| `ui/panels/sources.test.tsx`             | the sources dialog: a tab per source, the tab an auth failure lands on, and where the credential promise sits                    |
| `nodes/annotation/note.test.ts`          | that a text note is never executed, deferred, stalled or counted, and round-trips                                                |
| `store/links.test.ts`                    | breaking and re-routing links: one undo step, the id kept, and what a refused rewire leaves                                      |
| `core/splice.test.ts`                    | dropping a node on a wire: the downstream check needing the upstream link applied, isolation, and one link left on the port |
| `store/splice.test.ts`                   | that the move and the rewire undo as one gesture, and that a node which does not fit is still moved                          |
| `ui/panels/edgeMenu.test.tsx`            | the link menu: that its header names the wire it is about, and that deleting leaves the nodes                                    |
| `ui/panels/nodeMenu.test.tsx`            | the node menu's two caches: Results rather than "cache", and Clear Cache gated on the node declaring one                        |
| `ui/viewers/tableSummary.test.tsx`       | the inspector's text readout: every column listed, an id kept whole, a long value cut but recoverable, and no dash for no rows |
| `ui/nodes/cacheAge.test.tsx`             | `cached 3d ago ⟳`: the age surviving a restored result, the click reaching both caches, and no threshold hiding a fresh one     |
| `data/datasetCache.test.ts`              | "everything cached for this dataset" as a question about keys: one version not claiming another's (`hemibrain:v1.2` vs `v1.2.1`), the oldest entry winning, and an entry predating the timestamp sidecar still being found |
| `ui/nodes/datasetCacheAge.test.tsx`      | the dataset card's `cached 3d ago ⟳`: that it reads the cache rather than the run, starts no download by being on screen, notices one another card started, and offers `no cache` as text rather than a button |
| `ui/nodes/runWarning.test.tsx`           | a run warning reaching the card in the line a refusal used to take, standing aside for a run error, reaching the inspector — and `nodeIssues`' ranking, which both surfaces read |
| `ui/nodes/noteCard.test.tsx`             | the note card: markdown as prose, no node chrome, Escape abandoning an edit, the frame toggle                                    |
| `ui/nodes/nodeHints.test.tsx`            | hints docked to a card: markdown as prose, tone and side, and — the half that fails silently — that dismissing one leaves the graph object *identical*, adds no undo step, survives a remount, and is keyed on the hint's text rather than its address |
| `nodes/lib/connectivityOps.test.ts`      | the traversal: the pre→post swap, the both-ends dedupe, no re-expansion, minWeight pruning — and the `Neuron Set` port's derivation: the id dtype taken from the connectivity schema, the cell copied rather than rebuilt, a seed with no surviving edge kept, the first non-empty type winning over the row that fixed the order; the `published` filter dropping an edge, exempting a seed, bounding the frontier and still resolving `both` within a round; and the left join that keeps the endpoint list and the neuron rows the same length |
| `nodes/query/connectivity.test.ts`       | the node: that it advertises the columns it builds — both ports, under both `Neuron Set` settings — that Hops reaches the source, that `full` re-keys rather than serving the derived table, and `Include fragments` — the lookup it issues, that it asks about partners and never seeds, and that a stored node with no key for it reads as ticked |
| `layout/layout.test.ts`                  | the ELK mapping and placement, the port convention and option keys against real ELK, and a network laid out by it                |
| `layout/routing.test.ts`                 | edge routes: a wire bent past a card, pinned sockets honoured, a partial measurement declined, and what stales a route           |
| `ui/edgeRoute.test.ts`                   | the wire's geometry: the fillet clamped to its own segment, and the ends staying on the sockets rather than on ELK's guess       |
| `ui/edgeRouting.test.tsx`                | both routings drawn, the hit path following the detour, and which wires are marked as having followed a route                    |
| `nodes/lib/pathOps.test.ts`              | the bidirectional search, the feed-forward prune, bottleneck ranking, and what truncation admits                                 |
| `nodes/query/paths.test.ts`              | the node end to end, and that the collapse reaches the _source_ rather than relabelling after                                    |
| `ui/panels/layoutControls.test.tsx`      | the four rail buttons, the bubble, what clears the auto-layout toggle, and routes dropping on a drag but not a param edit        |
| `store/lock.test.ts`                     | the canvas lock at the store: what a locked graph refuses (move, resize, arrange, add, wire, delete, duplicate, cut, paste, undo, an assistant plan) and what it still does (params, rename, mute, collapse, select, copy, open another graph) |
| `core/clipboard.test.ts`                 | the clipboard fragment: internal edges and whole frames only, positions kept absolute, and the reading — prose and half-JSON refused, a document whose every node this build dropped refused too (so the paste event stays somebody else's), a whole `.coda.json` accepted, and a bad handle repaired the way a file's is rather than by a second reader. Plus the merge: everything re-identified, the top-left corner placed at the point given, the offset when there is none |
| `ui/clipboard.test.tsx`                  | the three gestures and what they leave alone: a copy claimed and one abandoned to a live text selection or a focused field, a cut that undoes in one step, a paste placed at the canvas's point and selected, a repeat stepped rather than stacked, a non-graph paste not swallowed and not answered with a notice, and the lock's split — cut and paste refused with the reason, copy through |
| `ui/panels/lock.test.tsx`                | the lock on screen: the rail dimming around the one button that stays live, the toolbar, the palette rows and node rows, and the keys that say why rather than doing nothing |
| `ui/panels/fitSelected.test.tsx`         | Fit Selected: which ids the rail button, the `§` key and the palette command each ask React Flow to frame, and that an empty selection asks for nothing |
| `ui/viewers/scatterPlot.test.ts`         | the scales and the log drop, the point budget's stride, the trend in transformed space, and a lasso catching rows nothing drew   |
| `ui/viewers/scatterDraw.test.ts`         | marker geometry, the colour+shape batching, and the exported SVG                                                                 |
| `ui/viewers/scatterViewer.test.tsx`      | the scatter caption: every admission it makes, and which legend keys stand down in a card                                        |
| `ui/viewers/heatmapPlot.test.ts`         | the heatmap fold: a grid bounded by the plot, the strongest cell of a block surviving, the ramp table proved lossless, thinning; the transcribed palettes' shape and endpoints, viridis running the same way on both themes, a name from the other scale's list falling back to Coda's; the window — zoom about an anchor clamped to the matrix, the one-line floor, a folded matrix un-folding inside a window with the hit test and the ring agreeing by matrix index, labels un-thinning as lines gain pixels, named over their visible part, evenly spaced whatever the rounding of their edges, and keeping their names across a pan; the colour domain — a manual end replacing one side, a diverging pair staying symmetric, clamping past either end, and the log mapping proved monotonic, finite on negative data, equal to the exporters' `log10` form, and the same in the spec's buckets as in `normalize` |
| `nodes/lib/matrixShape.test.ts`          | the heatmap's Filter and Order tabs without Python: the slash opt-in and its optional closing slash, a bare term staying literal through `SMP001(a)`, negation, a lone `/` or `!` narrowing nothing, `takeMatrix` taking a subset and refusing an index outside the matrix; totals over finite cells, natural label order, one row or column by label, the follower matched by label and not by index, a missing key as a problem rather than a throw, the total not reading the colour scale |
| `nodes/output/heatmap.test.ts`           | the node's halves: the filter running before the sort with a case where the two orders differ, an empty result honoured where an uncompilable pattern leaves the axis whole; every colour param presentational and every order param not, the details switched off until an order is chosen, and the clustering driven through a stub of the bridge — a copy of the matrix, once per leading axis, the answer through the same plan |
| `ui/viewers/svgBuilders.test.ts`         | every synthesised export through the real serializer: one namespace declaration, one font, and a document that actually parses |
| `nodes/output/scatter.test.ts`           | the tap, id-vs-row-index selection, and that `Max points` stales nothing                                                         |
| `core/columnParams.test.ts`              | what a column picker may complain about: unknown-vs-empty schema, what `optional` changes, a plural keeping an unseen list, and a singular on its default resolving the same either side of a schema |
| `nodes/output/barChart.test.ts`          | the tap, that an unpicked column is a warning and not a refusal, and the stack-by-itself catch                                   |
| `nodes/output/charts.test.ts`            | the histogram, pie and box plot as nodes: each a tap, each keeping neurons-ness on `Selected`, and the param split — that the one column a selection resolves against reaches the cache key while every drawing knob does not, asserted through the scheduler rather than off the flags |
| `nodes/lib/chartSelection.test.ts`       | what a chart mark is called: the missing bucket, table order, the range codec's exact double round trip and its rejection of an unknown suffix, half-open bars with one closed top, and that `Number()`'s zeros are refused |
| `ui/viewers/histogramBins.test.ts`       | binning: the largest value inside the last bar, Sturges when the IQR is zero, the automatic cap, value-space edges from log-space bins, density by each bar's own width, and the raw count kept beside the plotted height |
| `ui/viewers/boxStats.test.ts`            | type-7 quantiles, the whisker at the data rather than at the fence, the outlier draw cap against the reported count, a zero bandwidth answering zeros, quartiles unmoved by a log axis, one peak shared across violins, the kernel's two-pointer window against the definition it replaced, and the swarm packing: no two marks overlapping, interlocked rather than stacked, and thinned in value order |
| `ui/viewers/pieLayout.test.ts`           | the tally's negatives and nulls, the fold by size whatever the display order, the residual remembering its members, and the full circle drawn as two arcs |
| `ui/viewers/chartViewers.test.tsx`       | the three charts in jsdom: mark counts, the full-height hit areas, the legend rule, the captions' admissions, the box plot's two orientations — rotated labels and the value axis running upwards as columns — the swarm inside its own band, and above all what a click writes back: a range for a bar, a label for a slice, the folded names for `Other` |
| `nodes/table/pivot.test.ts`              | the two outputs describing one pivot, and the wide schema arriving only by observation                                           |
| `nodes/table/groupBy.test.ts`            | the node's computed schema: infer publishing exactly what evaluate returns for one value column and for several, the aggregate renamed with the aggregation, `count` ignoring the value list, a column the schema lacks dropped through `ctx.columns`, and each picker's own warning |
| `nodes/table/unpivot.test.ts`            | the fold's node contract: the schema derived rather than observed, the neurons claim dropped when the id column is folded, an unset picker warning rather than refusing, and Keep falling back to everything when its only column is gone |
| `nodes/lib/tableFilter.test.ts`          | a header cell's grammar: a bare value following the column's dtype, the null rule, and every clause it drops rather than applies |
| `nodes/lib/tableEdits.test.ts`           | Edit Table's op: schema/value agreement over an added column and a widened one, the direction every failure errs in — a bare term and an unknown filter column each switching their own rule off — `""` clearing a cell where a blank field does nothing, and rules seeing what the ones above them wrote |
| `nodes/output/table.test.ts`             | the two ports: the tap kept whole, filtering staling the node while paging does not, and a bad clause refusing nothing           |
| `nodes/table/sample.test.ts`             | the four sampling modes, a draw reproduced from its seed, and the seed costing nothing in the other three                        |
| `nodes/table/combine.test.ts`            | the coalesce node: infer publishing what evaluate returns, an unset picker warning rather than refusing, and the chain into a Dataset |
| `nodes/table/dedupe.test.ts`             | the three `keep` modes, an empty picker comparing whole rows, row order kept, and a null told apart from the text "null"       |
| `nodes/analysis/compareConnectivity.test.ts` | the node: pairs of sockets kept adjacent, per-dataset params hidden past the count, two datasets named alike suffixed rather than collapsed onto one column, and infer publishing what evaluate returns at every arity |
| `nodes/table/qualifyIds.test.ts`         | the qualified id: that `isNeuronId` rejects what it mints — the property the whole form was chosen for — the qualify/unqualify round trip, a null left null rather than tagged, the first-separator split, and the kind kept so Stack Tables still takes it |
| `nodes/table/relabel.test.ts`            | the Relabel node: infer publishing the dtype evaluate returns including `keep`'s widening, the `unmatched` default asserted as the design, both pickers' defaults landing on the mapper's Labels output, the id-drift warning, and a refusal where an `out.*` viewer would pass through |
| `nodes/table/rename.test.ts`             | the node: infer publishing the kind evaluate returns, promotion needing an applied rename, demotion said out loud, and four warnings that refuse nothing |
| `data/csv.test.ts`                       | reading somebody else's file: quoting, delimiter-by-consistency, header bias, a suffix that cannot land on a name already taken, and every value the parse refuses to widen |
| `data/uploads.test.ts`                   | the store against real IndexedDB: content addressing incl. a separator collision, a write that rejects, and the peek's one read  |
| `nodes/table/upload.test.ts`             | the node: the schema arriving by peek, the neuronId rename, what a graph opened elsewhere says, and the filename costing nothing   |
| `ui/nodes/uploadBody.test.tsx`           | the card's four states — and that 'looking' is never printed as 'not here' — plus the size ceiling refusing before it reads      |
| `nodes/table/fromUrl.test.ts`            | the fetch: deferred by the auto pass, Refresh as the only re-fetch, the schema keyed by URL, and what each refusal blames        |
| `nodes/lib/idList.test.ts`               | reading pasted ids: every separator, a bad token refusing the list, an eighteen-digit id kept exactly, and the 19-digit ceiling  |
| `nodes/query/inputIds.test.ts`           | the node: no dataset means no query, the seam asked in numbers, no status filter, empty never all                                |
| `ui/nodes/inputIdsBody.test.tsx`         | the card: the counts, the ids named as missing, and that it claims nothing with no Dataset wired                                 |
| `nodes/table/stack.test.ts`              | the union schema needing both sides, a real dtype clash refused at run time, and the source column                               |
| `ui/fullscreen.test.tsx`                 | the ⛶ and `F`: what element is handed over, that a refusal leaves the button unpressed, and the manifest's relative scope        |
| `ui/exportValue.test.ts`                 | SWC's 1-based ids and -1 roots, OBJ's 1-based faces, the JSON typed-array unpack, the file cap, and GraphML parsed as real XML   |
| `ui/nodes/resultDownload.test.tsx`       | the card foot's ⤓: absent before a run, withheld on a viewer card, the formats offered, and the dataset card the one rule reaches |
| `nodes/output/download.test.ts`          | the tap: identity pass-through, deferred by the auto pass, and settings re-running nothing                                       |
| `ui/useDownloads.test.tsx`               | the side effect: written on an executing run, not on an unchanged one, and the auto-run warning                                  |
| `ui/panels/startPage.test.tsx`           | (also) the field-guide links, in the welcome bar and the Help menu, composed against `BASE_URL`                                  |
| `nodes/lib/datasetStats.test.ts`         | the dataset roll-ups: null-vs-empty as one absence, absence counted apart, the residual fold, and one walk serving every cap        |
| `nodes/lib/datasetStats.test.ts`         | (also) which fields get a chart: the priority order, one slot per fact, numeric columns out, a chosen list taken literally, and `add` appending without the cap, the family rule, or a second copy of a name |
| `nodes/query/roiSummary.test.ts`         | the two ROI nodes: answering from a Dataset alone, a region kept when its summability is unknown, and the asymmetry between them   |
| `nodes/output/datasetSummary.test.ts`    | that a chart setting stales nothing while `Status` does, that it emits no ports at all, and that a graph stored before `chartsMode` existed loads meaning the whole list |
| `ui/viewers/datasetSummary.test.tsx`     | the card: absent tiles vs dashed ones, why an empty one is empty, the caption naming its population, ring-vs-bars, paging, and a named chart drawn beside the automatic ones or in place of them |
| `ui/useNeuronIndex.test.tsx`             | one load across two widgets, a late mount with no spinner flash, and a reload reaching every subscriber                            |
| `ui/raster.test.ts`                      | triangles and lines to masks, and masks to outlines: clipping, brightest-wins, a concavity kept, split blobs, the closed-ring simplify — and `drawSegment`'s three properties a quad-and-fill would not have: an unbroken diagonal, a sub-pixel segment that still marks its ends, and thickness read as a *diameter* so an even width does not collapse to one pixel |
| `ui/viewers/roiProjection.test.ts`       | the three anatomical planes, an outline that keeps its notch, the explode proved non-uniform, the frame held at full, and mesh volume |
| `data/obj.test.ts`                       | reading somebody else's OBJ: every face-index form agreeing, polygons fanned, CRLF, and what a 200 that is not a mesh says |
| `nodes/output/rois.test.ts`              | the ROI Viewer: no ports at all, every control costing no run, and what it says with a source publishing no region meshes |
| `data/meshDecimate.test.ts`              | vertex clustering: the silhouette surviving, no degenerate faces, determinism, and welding a seam counted apart from decimating |
| `ui/viewers/roiOutlines.test.ts`         | what survives when the meshes are released: three planes per region, the size claim, and a fingerprint that re-fetches on a changed region list |
| `ui/viewers/rois.test.tsx`               | the card through `ValuePreview`: that it asks before spending 60 MB, stops asking once cached, drops the rail when compact, and marks the volume an estimate |
| `ui/viewers/roiStyle.test.ts`            | the region hue: a left/right pair sharing one, sub-regions distinct, literal hex, and a published ratio clamped |
| `data/neuprint/roiHierarchy.test.ts`     | the level above primary: groups from the real hemibrain tree, the root excluded, sub-primary not descended into, and a malformed tree losing the grouping not the map |
| `nodes/transform/selectOne.test.ts`      | stepping free vs committing stale, an index past the end emitting nothing, and one skeleton re-measuring its bounds                |
| `ui/nodes/selectOneBody.test.tsx`        | the pager card: not-run vs not-wired, the Live label counted once, and the gap between what is shown and what is emitted           |
| `ui/nodes/renameBody.test.tsx`           | the rename card: Add drawing a row without writing one, a filled row stored as a JSON pair, and the picker never disabled where the schema has not arrived |
| `ui/nodes/editTableBody.test.tsx`        | the Edit Table card: Add drawing a row without writing one, a filled rule stored as one JSON object, the column staying a text field with completions rather than a picker, and a rule that changes nothing marked on the field carrying the reason |
| `nodeguide/nodeGuide.test.ts`            | the node guide's data: a paragraph per node, socket styles, internal-vs-advanced params, an enum's label, and the examples cross-reference |
| `overview/overview.test.ts`              | the overview page against the registry: the node count it advertises as a floor, and every backend and real dataset family named on it |
